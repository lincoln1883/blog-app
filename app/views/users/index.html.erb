<div class="w-full h-full flex-col ">
  <% @user.each do |user| %>
    <%= link_to user_path(user), remote: true do %>
      <div class="flex flex-col gap-2 mt-4 items-center bg-white border border-gray-200 rounded-lg shadow md:flex-row
       md:w-auto hover:bg-gray-100 dark:border-gray-700">
        <img class="object-cover h-full w-full rounded-t-lg h-96 md:h-auto md:w-48
         md:rounded-none md:rounded-l-lg" src="<%= user.photo %>" alt="user image">
        <div class="flex flex-col justify-between p-4 leading-normal w-full h-40">
          <h5 class="mb-2 text-4xl font-bold tracking-tight"><%= user.name %></h5>
          <p class="mb-3 text-right font-normal">number of posts: <%= user.posts.count %></p>
        </div>
      </div>
    <% end %>
  <% end %>
</div>
