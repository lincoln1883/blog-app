<div>
  <h1>Users index</h1>
  <div>
    <% @user.each do |user| %>
      <img src="<%= user.photo %>" alt="user image">
      <h3><%= user.name %></h3>
      <p>number of posts: <%= user.posts.count %></p>
    <% end %>
  </div>
</div>